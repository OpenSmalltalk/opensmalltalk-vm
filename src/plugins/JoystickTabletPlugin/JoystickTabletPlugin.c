/* Automatically generated by
	SmartSyntaxPluginCodeGenerator VMMaker.oscog-eem.3166 uuid: bda1c176-bedf-4490-bb95-90d3851c1526
   from
	JoystickTabletPlugin VMMaker.oscog-eem.3166 uuid: bda1c176-bedf-4490-bb95-90d3851c1526
 */
static char __buildInfo[] = "JoystickTabletPlugin VMMaker.oscog-eem.3166 uuid: bda1c176-bedf-4490-bb95-90d3851c1526 " __DATE__ ;


#include "config.h"
#include <math.h>
#include "sqMathShim.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

/* Do not include the entire sq.h file but just those parts needed. */
#include "sqConfig.h"			/* Configuration options */
#include "sqVirtualMachine.h"	/*  The virtual machine proxy definition */
#include "sqPlatformSpecific.h"	/* Platform specific definitions */

#include "JoystickTabletPlugin.h"
#include "sqMemoryAccess.h"

#define true 1
#define false 0
#define null 0  /* using 'null' because nil is predefined in Think C */
#ifdef SQUEAK_BUILTIN_PLUGIN
# undef EXPORT
# define EXPORT(returnType) static returnType
# define INT_EXT "(i)"
#else
# define INT_EXT "(e)"
#endif


/*** Function Prototypes ***/
EXPORT(const char*) getModuleName(void);
EXPORT(sqInt) initialiseModule(void);
EXPORT(sqInt) primitiveGetTabletParameters(void);
EXPORT(sqInt) primitiveReadJoystick(void);
EXPORT(sqInt) primitiveReadTablet(void);
EXPORT(sqInt) setInterpreter(struct VirtualMachine *anInterpreter);
EXPORT(sqInt) shutdownModule(void);


/*** Variables ***/

#if !defined(SQUEAK_BUILTIN_PLUGIN)
static sqInt (*classBitmap)(void);
static sqInt (*failed)(void);
static void * (*firstIndexableField)(sqInt oop);
static sqInt (*instantiateClassindexableSize)(sqInt classPointer, sqInt size);
#if !defined(integerValueOf)
static sqInt (*integerValueOf)(sqInt oop);
#endif
#if !defined(isIntegerObject)
static sqInt (*isIntegerObject)(sqInt objectPointer);
#endif
static sqInt (*methodReturnValue)(sqInt oop);
static sqInt (*positive32BitIntegerFor)(unsigned int integerValue);
static sqInt (*primitiveFailFor)(sqInt reasonCode);
static sqInt (*stackValue)(sqInt offset);
static sqInt (*success)(sqInt aBoolean);
#else /* !defined(SQUEAK_BUILTIN_PLUGIN) */
extern sqInt classBitmap(void);
extern sqInt failed(void);
extern void * firstIndexableField(sqInt oop);
extern sqInt instantiateClassindexableSize(sqInt classPointer, sqInt size);
#if !defined(integerValueOf)
extern sqInt integerValueOf(sqInt oop);
#endif
#if !defined(isIntegerObject)
extern sqInt isIntegerObject(sqInt objectPointer);
#endif
extern sqInt methodReturnValue(sqInt oop);
extern sqInt positive32BitIntegerFor(unsigned int integerValue);
extern sqInt primitiveFailFor(sqInt reasonCode);
extern sqInt stackValue(sqInt offset);
extern sqInt success(sqInt aBoolean);
extern
#endif
struct VirtualMachine* interpreterProxy;
static const char *moduleName = "JoystickTabletPlugin VMMaker.oscog-eem.3166 " INT_EXT;



/*	Note: This is hardcoded so it can be run from Squeak.
	The module name is used for validating a module *after*
	it is loaded to check if it does really contain the module
	we're thinking it contains. This is important! */

	/* InterpreterPlugin>>#getModuleName */
EXPORT(const char*)
getModuleName(void)
{
	return moduleName;
}

	/* JoystickTabletPlugin>>#initialiseModule */
EXPORT(sqInt)
initialiseModule(void)
{
	return joystickInit();
}


/*	Get information on the pen tablet attached to this machine. Fail if there
	is no tablet. If successful, the result is an array of integers; see the
	Smalltalk call on this primitive for its interpretation.
 */

	/* JoystickTabletPlugin>>#primitiveGetTabletParameters: */
EXPORT(sqInt)
primitiveGetTabletParameters(void)
{
	sqInt cursorIndex;
	sqInt result;
	int *resultPtr;
	sqInt resultSize;

	if (!(isIntegerObject((cursorIndex = stackValue(0))))) {
		return primitiveFailFor(PrimErrBadArgument);
	}
	cursorIndex = integerValueOf(cursorIndex);
	resultSize = tabletResultSize();
	result = instantiateClassindexableSize(classBitmap(), resultSize);
	resultPtr = ((int *)firstIndexableField(result));
	success(tabletGetParameters(cursorIndex, resultPtr));
	if (!(failed())) {
		methodReturnValue(result);
	}
	return null;
}


/*	Read an input word from the joystick with the given index. */

	/* JoystickTabletPlugin>>#primitiveReadJoystick: */
EXPORT(sqInt)
primitiveReadJoystick(void)
{
	sqInt index;

	if (!(isIntegerObject((index = stackValue(0))))) {
		return primitiveFailFor(PrimErrBadArgument);
	}
	index = integerValueOf(index);
	if (!(failed())) {
		methodReturnValue(positive32BitIntegerFor((joystickRead(index))));
	}
	return null;
}


/*	Get the current state of the cursor of the pen tablet specified by my
	argument. Fail if there is no tablet. If successful, the result is an
	array of integers; see the Smalltalk call on this primitive for its
	interpretation. 
 */

	/* JoystickTabletPlugin>>#primitiveReadTablet: */
EXPORT(sqInt)
primitiveReadTablet(void)
{
	sqInt cursorIndex;
	sqInt result;
	int *resultPtr;
	sqInt resultSize;

	if (!(isIntegerObject((cursorIndex = stackValue(0))))) {
		return primitiveFailFor(PrimErrBadArgument);
	}
	cursorIndex = integerValueOf(cursorIndex);
	resultSize = tabletResultSize();
	result = instantiateClassindexableSize(classBitmap(), resultSize);
	resultPtr = ((int *)firstIndexableField(result));
	success(tabletRead(cursorIndex, resultPtr));
	if (!(failed())) {
		methodReturnValue(result);
	}
	return null;
}


/*	Note: This is coded so that it can be run in Squeak. */

	/* InterpreterPlugin>>#setInterpreter: */
EXPORT(sqInt)
setInterpreter(struct VirtualMachine *anInterpreter)
{
	sqInt ok;


	/* This may seem tautological, but in a real plugin it checks that the VM provides
	   the version the plugin was compiled against which is the version the plugin expects. */
	interpreterProxy = anInterpreter;
	ok = ((interpreterProxy->majorVersion()) == (VM_PROXY_MAJOR))
	 && ((interpreterProxy->minorVersion()) >= (VM_PROXY_MINOR));
	if (ok) {
		
#if !defined(SQUEAK_BUILTIN_PLUGIN)
		classBitmap = interpreterProxy->classBitmap;
		failed = interpreterProxy->failed;
		firstIndexableField = interpreterProxy->firstIndexableField;
		instantiateClassindexableSize = interpreterProxy->instantiateClassindexableSize;
#if !defined(integerValueOf)
		integerValueOf = interpreterProxy->integerValueOf;
#endif
#if !defined(isIntegerObject)
		isIntegerObject = interpreterProxy->isIntegerObject;
#endif
		methodReturnValue = interpreterProxy->methodReturnValue;
		positive32BitIntegerFor = interpreterProxy->positive32BitIntegerFor;
		primitiveFailFor = interpreterProxy->primitiveFailFor;
		stackValue = interpreterProxy->stackValue;
		success = interpreterProxy->success;
#endif /* !defined(SQUEAK_BUILTIN_PLUGIN) */
	}
	return ok;
}

	/* JoystickTabletPlugin>>#shutdownModule */
EXPORT(sqInt)
shutdownModule(void)
{
	return joystickShutdown();
}


#ifdef SQUEAK_BUILTIN_PLUGIN

static char _m[] = "JoystickTabletPlugin";
void* JoystickTabletPlugin_exports[][3] = {
	{(void*)_m, "getModuleName", (void*)getModuleName},
	{(void*)_m, "initialiseModule", (void*)initialiseModule},
	{(void*)_m, "primitiveGetTabletParameters\000\000\000", (void*)primitiveGetTabletParameters},
	{(void*)_m, "primitiveReadJoystick\000\000\000", (void*)primitiveReadJoystick},
	{(void*)_m, "primitiveReadTablet\000\000\000", (void*)primitiveReadTablet},
	{(void*)_m, "setInterpreter", (void*)setInterpreter},
	{(void*)_m, "shutdownModule", (void*)shutdownModule},
	{NULL, NULL, NULL}
};

#else // ifdef SQ_BUILTIN_PLUGIN

#if SPURVM
EXPORT(signed short) primitiveGetTabletParametersMetadata = 0;
EXPORT(signed short) primitiveReadJoystickMetadata = 0;
EXPORT(signed short) primitiveReadTabletMetadata = 0;
#endif // SPURVM

#endif // ifdef SQ_BUILTIN_PLUGIN
