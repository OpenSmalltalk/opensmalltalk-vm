/* Automatically generated by
	VMPluginCodeGenerator VMMaker.oscog-eem.2995 uuid: d5fbffa6-5db7-4c4d-8937-a45036ab3015
   from
	DropPlugin VMMaker.oscog-eem.2995 uuid: d5fbffa6-5db7-4c4d-8937-a45036ab3015
 */
static char __buildInfo[] = "DropPlugin VMMaker.oscog-eem.2995 uuid: d5fbffa6-5db7-4c4d-8937-a45036ab3015 " __DATE__ ;


#include "config.h"
#include <math.h>
#include "sqMathShim.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

/* Do not include the entire sq.h file but just those parts needed. */
#include "sqConfig.h"			/* Configuration options */
#include "sqVirtualMachine.h"	/*  The virtual machine proxy definition */
#include "sqPlatformSpecific.h"	/* Platform specific definitions */

#include "DropPlugin.h"
#include "sqMemoryAccess.h"

#define true 1
#define false 0
#define null 0  /* using 'null' because nil is predefined in Think C */
#ifdef SQUEAK_BUILTIN_PLUGIN
# undef EXPORT
# define EXPORT(returnType) static returnType
# define INT_EXT "(i)"
#else
# define INT_EXT "(e)"
#endif


/*** Function Prototypes ***/
EXPORT(const char*) getModuleName(void);
EXPORT(sqInt) initialiseModule(void);
EXPORT(sqInt) primitiveDropRequestFileHandle(void);
EXPORT(sqInt) primitiveDropRequestFileName(void);
EXPORT(sqInt) primitiveDropRequestURI(void);
EXPORT(sqInt) setInterpreter(struct VirtualMachine *anInterpreter);
EXPORT(sqInt) shutdownModule(void);


/*** Variables ***/

#if !defined(SQUEAK_BUILTIN_PLUGIN)
static sqInt (*failed)(void);
static sqInt (*methodArgumentCount)(void);
static sqInt (*methodReturnString)(char *aCString);
static sqInt (*methodReturnValue)(sqInt oop);
static sqInt (*primitiveFail)(void);
static sqInt (*stackIntegerValue)(sqInt offset);
#else /* !defined(SQUEAK_BUILTIN_PLUGIN) */
extern sqInt failed(void);
extern sqInt methodArgumentCount(void);
extern sqInt methodReturnString(char *aCString);
extern sqInt methodReturnValue(sqInt oop);
extern sqInt primitiveFail(void);
extern sqInt stackIntegerValue(sqInt offset);
extern
#endif
struct VirtualMachine* interpreterProxy;
static const char *moduleName = "DropPlugin VMMaker.oscog-eem.2995 " INT_EXT;



/*	Note: This is hardcoded so it can be run from Squeak.
	The module name is used for validating a module *after*
	it is loaded to check if it does really contain the module
	we're thinking it contains. This is important! */

	/* InterpreterPlugin>>#getModuleName */
EXPORT(const char*)
getModuleName(void)
{
	return moduleName;
}

	/* DropPlugin>>#initialiseModule */
EXPORT(sqInt)
initialiseModule(void)
{
	return dropInit();
}


/*	Note: File handle creation needs to be handled by specific support code
	explicitly bypassing the plugin file sand box.
 */

	/* DropPlugin>>#primitiveDropRequestFileHandle */
EXPORT(sqInt)
primitiveDropRequestFileHandle(void)
{
    sqInt dropIndex;
    sqInt handleOop;

	if (!((methodArgumentCount()) == 1)) {
		return primitiveFail();
	}
	dropIndex = stackIntegerValue(0);
	if (!(failed())) {
		handleOop = dropRequestFileHandle(dropIndex);
		if (!(failed())) {
			methodReturnValue(handleOop);
		}
	}
	return 0;
}


/*	Note: File handle creation needs to be handled by specific support code
	explicitly bypassing the plugin file sand box.
 */

	/* DropPlugin>>#primitiveDropRequestFileName */
EXPORT(sqInt)
primitiveDropRequestFileName(void)
{
    sqInt dropIndex;

	if (!((methodArgumentCount()) == 1)) {
		return primitiveFail();
	}

	/* dropRequestFileName returns name or NULL on error; methodReturnString: fails for nil */
	dropIndex = stackIntegerValue(0);
	if (!(failed())) {
		methodReturnString(dropRequestFileName(dropIndex));
	}
	return 0;
}


/*	On platforms that don't support URIs (currently Windows) the idea is to
	use primitiveDropRequestURI,
	which will fail, and then fall back on primitiveDropRequestFileName. */

	/* DropPlugin>>#primitiveDropRequestURI */
EXPORT(sqInt)
primitiveDropRequestURI(void)
{
    sqInt dropIndex;

	if (!((methodArgumentCount()) == 1)) {
		return primitiveFail();
	}

	/* dropRequestURI returns name or NULL on error; methodReturnString: fails for nil */
	dropIndex = stackIntegerValue(0);
	if (!(failed())) {
		methodReturnString(dropRequestURI(dropIndex));
	}
	return 0;
}


/*	Note: This is coded so that it can be run in Squeak. */

	/* InterpreterPlugin>>#setInterpreter: */
EXPORT(sqInt)
setInterpreter(struct VirtualMachine *anInterpreter)
{
    sqInt ok;


	/* This may seem tautological, but in a real plugin it checks that the VM provides
	   the version the plugin was compiled against which is the version the plugin expects. */
	interpreterProxy = anInterpreter;
	ok = ((interpreterProxy->majorVersion()) == (VM_PROXY_MAJOR))
	 && ((interpreterProxy->minorVersion()) >= (VM_PROXY_MINOR));
	if (ok) {
		
#if !defined(SQUEAK_BUILTIN_PLUGIN)
		failed = interpreterProxy->failed;
		methodArgumentCount = interpreterProxy->methodArgumentCount;
		methodReturnString = interpreterProxy->methodReturnString;
		methodReturnValue = interpreterProxy->methodReturnValue;
		primitiveFail = interpreterProxy->primitiveFail;
		stackIntegerValue = interpreterProxy->stackIntegerValue;
#endif /* !defined(SQUEAK_BUILTIN_PLUGIN) */
	}
	return ok;
}

	/* DropPlugin>>#shutdownModule */
EXPORT(sqInt)
shutdownModule(void)
{
	return dropShutdown();
}


#ifdef SQUEAK_BUILTIN_PLUGIN

static char _m[] = "DropPlugin";
void* DropPlugin_exports[][3] = {
	{(void*)_m, "getModuleName", (void*)getModuleName},
	{(void*)_m, "initialiseModule", (void*)initialiseModule},
	{(void*)_m, "primitiveDropRequestFileHandle\000\000\000", (void*)primitiveDropRequestFileHandle},
	{(void*)_m, "primitiveDropRequestFileName\000\000\000", (void*)primitiveDropRequestFileName},
	{(void*)_m, "primitiveDropRequestURI\000\000\000", (void*)primitiveDropRequestURI},
	{(void*)_m, "setInterpreter", (void*)setInterpreter},
	{(void*)_m, "shutdownModule", (void*)shutdownModule},
	{NULL, NULL, NULL}
};

#else /* ifdef SQ_BUILTIN_PLUGIN */

EXPORT(signed short) primitiveDropRequestFileHandleMetadata = 0;
EXPORT(signed short) primitiveDropRequestFileNameMetadata = 0;
EXPORT(signed short) primitiveDropRequestURIMetadata = 0;

#endif /* ifdef SQ_BUILTIN_PLUGIN */
