/* Automatically generated by
	SmartSyntaxPluginCodeGenerator * VMMaker.oscog-eem.3636 uuid: 152db94e-b042-4e5b-a8aa-ac1b663ffe2a
	(Compiler-ct.519)
   from
	PseudoTTYPlugin VMConstruction-Plugins-PseudoTTYPlugin-dtl.5 uuid: 47f24fad-e652-4427-9f4e-6fad746c4c55
 */
static char __buildInfo[] = "PseudoTTYPlugin VMConstruction-Plugins-PseudoTTYPlugin-dtl.5 uuid: 47f24fad-e652-4427-9f4e-6fad746c4c55 " __DATE__ ;


#include "config.h"
#include <math.h>
#include "sqMathShim.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

/* Do not include the entire sq.h file but just those parts needed. */
#include "sqConfig.h"			/* Configuration options */
#include "sqVirtualMachine.h"	/*  The virtual machine proxy definition */
#include "sqPlatformSpecific.h"	/* Platform specific definitions */

#include "PseudoTTYPlugin.h"
#include "sqMemoryAccess.h"

#define true 1
#define false 0
#define null 0  /* using 'null' because nil is predefined in Think C */
#ifdef SQUEAK_BUILTIN_PLUGIN
# undef EXPORT
# define EXPORT(returnType) static returnType
# define INT_EXT "(i)"
#else
# define INT_EXT "(e)"
#endif


/*** Function Prototypes ***/
static AsyncFile * asyncFileValueOf(sqInt oop);
EXPORT(const char*) getModuleName(void);
EXPORT(sqInt) initialiseModule(void);
EXPORT(sqInt) primPtyClose(void);
EXPORT(sqInt) primPtyForkAndExec(void);
EXPORT(sqInt) primPtyForkChildAndExec(void);
EXPORT(sqInt) primPtyWindowSize(void);
EXPORT(sqInt) setInterpreter(struct VirtualMachine *anInterpreter);
EXPORT(sqInt) shutdownModule(void);


/*** Variables ***/

#if !defined(SQUEAK_BUILTIN_PLUGIN)
static sqInt (*classByteArray)(void);
static sqInt (*failed)(void);
static void * (*firstIndexableField)(sqInt oop);
static sqInt (*instantiateClassindexableSize)(sqInt classPointer, sqInt size);
#if !defined(integerObjectOf)
static sqInt (*integerObjectOf)(sqInt value);
#endif
#if !defined(integerValueOf)
static sqInt (*integerValueOf)(sqInt oop);
#endif
static void * (*ioLoadFunctionFrom)(char *functionName, char *moduleName);
static sqInt (*isBytes)(sqInt oop);
#if !defined(isIntegerObject)
static sqInt (*isIntegerObject)(sqInt objectPointer);
#endif
static sqInt (*isPointers)(sqInt oop);
static sqInt (*methodReturnValue)(sqInt oop);
static sqInt (*pop)(sqInt nItems);
static sqInt (*popRemappableOop)(void);
static sqInt (*primitiveFailFor)(sqInt reasonCode);
static sqInt (*pushRemappableOop)(sqInt oop);
static sqInt (*slotSizeOf)(sqInt oop);
static sqInt (*stObjectatput)(sqInt array, sqInt index, sqInt value);
static sqInt (*stackValue)(sqInt offset);
static sqInt (*success)(sqInt aBoolean);
#else /* !defined(SQUEAK_BUILTIN_PLUGIN) */
extern sqInt classByteArray(void);
extern sqInt failed(void);
extern void * firstIndexableField(sqInt oop);
extern sqInt instantiateClassindexableSize(sqInt classPointer, sqInt size);
#if !defined(integerObjectOf)
extern sqInt integerObjectOf(sqInt value);
#endif
#if !defined(integerValueOf)
extern sqInt integerValueOf(sqInt oop);
#endif
extern void * ioLoadFunctionFrom(char *functionName, char *moduleName);
extern sqInt isBytes(sqInt oop);
#if !defined(isIntegerObject)
extern sqInt isIntegerObject(sqInt objectPointer);
#endif
extern sqInt isPointers(sqInt oop);
extern sqInt methodReturnValue(sqInt oop);
extern sqInt pop(sqInt nItems);
extern sqInt popRemappableOop(void);
extern sqInt primitiveFailFor(sqInt reasonCode);
extern sqInt pushRemappableOop(sqInt oop);
extern sqInt slotSizeOf(sqInt oop);
extern sqInt stObjectatput(sqInt array, sqInt index, sqInt value);
extern sqInt stackValue(sqInt offset);
extern sqInt success(sqInt aBoolean);
extern
#endif
struct VirtualMachine* interpreterProxy;
static const char *moduleName = "PseudoTTYPlugin VMConstruction-Plugins-PseudoTTYPlugin-dtl.5 " INT_EXT;
static sqInt sCOAFfn;


/*** Methods ***/


/*	Return a pointer to the first byte of the async file record within the
	given Smalltalk bytes object, or nil if oop is not an async file record.
 */

	/* PseudoTTYPlugin>>#asyncFileValueOf: */
static AsyncFile *
asyncFileValueOf(sqInt oop)
{
	success((!(isIntegerObject(oop)))
	 && ((isBytes(oop))
	 && ((slotSizeOf(oop)) == (sizeof(AsyncFile)))));
	if (failed()) {
		return null;
	}
	return ((AsyncFile *) (firstIndexableField(oop)));
}


/*	Note: This is hardcoded so it can be run from Squeak.
	The module name is used for validating a module *after*
	it is loaded to check if it does really contain the module
	we're thinking it contains. This is important! */

	/* InterpreterPlugin>>#getModuleName */
EXPORT(const char*)
getModuleName(void)
{
	return moduleName;
}


/*	We have to load AsyncFile first, to get the sessionID. */

	/* PseudoTTYPlugin>>#initialiseModule */
EXPORT(sqInt)
initialiseModule(void)
{
	ioLoadFunctionFrom("initializeModule", "AsynchFilePlugin");
	return ptyInit();
}

	/* PseudoTTYPlugin>>#primitivePtyClose: */
EXPORT(sqInt)
primPtyClose(void)
{
	AsyncFile *f;
	sqInt fHandle;

	fHandle = stackValue(0);
	f = asyncFileValueOf(fHandle);
	if (!(failed())) {
		ptyClose(f);
	}
	if (!(failed())) {
		pop(1);
	}
	return null;
}

	/* PseudoTTYPlugin>>#primitivePtyForkAndExec:arguments:semaIndex: */
EXPORT(sqInt)
primPtyForkAndExec(void)
{
	char *argIdx;
	sqInt argLen;
	sqInt args;
	sqInt cmd;
	char *cmdIdx;
	sqInt cmdLen;
	AsyncFile *f;
	sqInt fOop;
	sqInt semaIndex;

	if (!(isIntegerObject((semaIndex = stackValue(0))))) {
		return primitiveFailFor(PrimErrBadArgument);
	}
	cmd = stackValue(2);
	args = stackValue(1);
	semaIndex = integerValueOf(semaIndex);
	success(isBytes(cmd));
	success(isPointers(args));
	if (failed()) {
		return null;
	}
	cmdIdx = firstIndexableField(cmd);

	/* in bytes */
	cmdLen = slotSizeOf(cmd);
	argIdx = firstIndexableField(args);

	/* in fields */
	argLen = slotSizeOf(args);
	fOop = instantiateClassindexableSize(classByteArray(), sizeof(AsyncFile));
	f = asyncFileValueOf(fOop);
	if (!(failed())) {
		ptyForkAndExec(f, semaIndex, cmdIdx, cmdLen, argIdx, argLen);
	}
	if (!(failed())) {
		methodReturnValue(fOop);
	}
	return null;
}


/*	Answer a handle for an async file connected to the forked process on a
	pty, and store
	the pid of the forked child process in singleObjectContainer, which is an
	array of size
	one or any similar object.
 */

	/* PseudoTTYPlugin>>#primitivePtyForkAndExec:arguments:semaIndex:childPidContainer: */
EXPORT(sqInt)
primPtyForkChildAndExec(void)
{
	char *argIdx;
	sqInt argLen;
	sqInt args;
	pid_t childPid;
	sqInt cmd;
	char *cmdIdx;
	sqInt cmdLen;
	AsyncFile *f;
	sqInt fOop;
	sqInt pidContainer;
	sqInt semaIndex;
	sqInt singleObjectContainer;

	if (!(isIntegerObject((semaIndex = stackValue(1))))) {
		return primitiveFailFor(PrimErrBadArgument);
	}
	cmd = stackValue(3);
	args = stackValue(2);
	semaIndex = integerValueOf(semaIndex);
	singleObjectContainer = stackValue(0);
	success(isBytes(cmd));
	success(isPointers(args));
	success((isPointers(singleObjectContainer))
	 && ((slotSizeOf(singleObjectContainer)) > 0));
	if (failed()) {
		return null;
	}
	cmdIdx = firstIndexableField(cmd);

	/* in bytes */
	cmdLen = slotSizeOf(cmd);
	argIdx = firstIndexableField(args);

	/* in fields */
	argLen = slotSizeOf(args);
	pushRemappableOop(singleObjectContainer);
	fOop = instantiateClassindexableSize(classByteArray(), sizeof(AsyncFile));
	pidContainer = popRemappableOop();
	f = asyncFileValueOf(fOop);
	if (failed()) {
		childPid = -1;
	}
	else {
		childPid = ptyForkAndExec(f, semaIndex, cmdIdx, cmdLen, argIdx, argLen);
	}
	stObjectatput(pidContainer, 1, integerObjectOf(childPid));
	if (!(failed())) {
		methodReturnValue(fOop);
	}
	return null;
}

	/* PseudoTTYPlugin>>#primitivePtyWindowSize:cols:rows: */
EXPORT(sqInt)
primPtyWindowSize(void)
{
	sqInt cols;
	AsyncFile *f;
	sqInt fHandle;
	sqInt rows;

	if (!((isIntegerObject((cols = stackValue(1))))
		 && (isIntegerObject((rows = stackValue(0)))))) {
		return primitiveFailFor(PrimErrBadArgument);
	}
	fHandle = stackValue(2);
	cols = integerValueOf(cols);
	rows = integerValueOf(rows);
	f = asyncFileValueOf(fHandle);
	if (!(failed())) {
		ptyWindowSize(f, cols, rows);
	}
	if (!(failed())) {
		pop(3);
	}
	return null;
}


/*	Note: This is coded so that it can be run in Squeak. */

	/* InterpreterPlugin>>#setInterpreter: */
EXPORT(sqInt)
setInterpreter(struct VirtualMachine *anInterpreter)
{
	sqInt ok;

	interpreterProxy = anInterpreter;

	/* This may seem tautological, but in a real plugin it checks that the VM provides
	   the version the plugin was compiled against which is the version the plugin expects. */
	ok = ((interpreterProxy->majorVersion()) == (VM_PROXY_MAJOR))
		 && ((interpreterProxy->minorVersion()) >= (VM_PROXY_MINOR));
	if (ok) {
		
#if !defined(SQUEAK_BUILTIN_PLUGIN)
		classByteArray = interpreterProxy->classByteArray;
		failed = interpreterProxy->failed;
		firstIndexableField = interpreterProxy->firstIndexableField;
		instantiateClassindexableSize = interpreterProxy->instantiateClassindexableSize;
#if !defined(integerObjectOf)
		integerObjectOf = interpreterProxy->integerObjectOf;
#endif
#if !defined(integerValueOf)
		integerValueOf = interpreterProxy->integerValueOf;
#endif
		ioLoadFunctionFrom = interpreterProxy->ioLoadFunctionFrom;
		isBytes = interpreterProxy->isBytes;
#if !defined(isIntegerObject)
		isIntegerObject = interpreterProxy->isIntegerObject;
#endif
		isPointers = interpreterProxy->isPointers;
		methodReturnValue = interpreterProxy->methodReturnValue;
		pop = interpreterProxy->pop;
		popRemappableOop = interpreterProxy->popRemappableOop;
		primitiveFailFor = interpreterProxy->primitiveFailFor;
		pushRemappableOop = interpreterProxy->pushRemappableOop;
		slotSizeOf = interpreterProxy->slotSizeOf;
		stObjectatput = interpreterProxy->stObjectatput;
		stackValue = interpreterProxy->stackValue;
		success = interpreterProxy->success;
#endif /* !defined(SQUEAK_BUILTIN_PLUGIN) */
	}
	return ok;
}

	/* PseudoTTYPlugin>>#shutdownModule */
EXPORT(sqInt)
shutdownModule(void)
{
	return ptyShutdown();
}

/*** Exports ***/

#ifdef SQUEAK_BUILTIN_PLUGIN

static char _m[] = "PseudoTTYPlugin";
void* PseudoTTYPlugin_exports[][3] = {
	{(void*)_m, "getModuleName", (void*)getModuleName},
	{(void*)_m, "initialiseModule", (void*)initialiseModule},
	{(void*)_m, "primPtyClose\000\001\000", (void*)primPtyClose},
	{(void*)_m, "primPtyForkAndExec\000\001\000", (void*)primPtyForkAndExec},
	{(void*)_m, "primPtyForkChildAndExec\000\001\000", (void*)primPtyForkChildAndExec},
	{(void*)_m, "primPtyWindowSize\000\001\000", (void*)primPtyWindowSize},
	{(void*)_m, "setInterpreter", (void*)setInterpreter},
	{(void*)_m, "shutdownModule", (void*)shutdownModule},
	{NULL, NULL, NULL}
};

#else // ifdef SQ_BUILTIN_PLUGIN

#if SPURVM
EXPORT(signed short) primPtyCloseMetadata = 0x100;
EXPORT(signed short) primPtyForkAndExecMetadata = 0x100;
EXPORT(signed short) primPtyForkChildAndExecMetadata = 0x100;
EXPORT(signed short) primPtyWindowSizeMetadata = 0x100;
#endif // SPURVM

#endif // ifdef SQ_BUILTIN_PLUGIN
